# 计划书: LM 长文本档 (token 级模型信源编码)

> 2026-09 · 征集协作者 · 领取方式: 在本文对应任务处署名, 分支开发 + PR
> 项目总览见 [README.md](README.md); 本文只讲「LM 档」这一条线。
> 实验脚本与复现方法见 [exp_minimind/](exp_minimind/README.md)。

## 0. 一分钟背景

游戏口琴 = 数据信道: 发送端把文本编成音符序列, 玩家照谱吹奏; 接收端采集
音频做音高检测, 还原符号流再解码。信道约束: 8 音环 C4..C5, 相邻不能同音
→ 旋转编码下 **1 音符携带 1 个 base-7 数字 (≈2.807 bit)**, 带宽 = 人工
吹奏 ≈ 3 音符/秒。

现状(已入库, 各模块有 selftest):

| 模块 | 内容 |
|---|---|
| `huffman_codec.py` | 字符级 order-0 七叉哈夫曼 + 旋转层, 3.42 数字/字 |
| `rs_codec.py` | GF(7) RS 纠错 + 停顿分帧 (no/medium/high 三档) |
| `receiver.py` | 流式贪心解码(永不崩溃) + 单错误枚举(实验性) |
| `frontend/` + `frontend/gui.py` | 音高检测 + 监视 GUI (项目主线) |

## 1. 为什么上模型 (实验结论, 2026-09)

用 MiniMind-3 (68.8M 参数, CPU 单线程可跑) 做「逐位置条件分布 → 七叉
哈夫曼树」的信源编码, 实测(`exp_minimind/`, 可复现):

| 方案 | 数字/字 | 备注 |
|---|---:|---|
| order-0 字符哈夫曼(现状) | 3.42 | 静态码表 |
| 字符级逐位置树 | 3.24 | 被 BPE 粒度锁死, 上限约 -15% |
| **token 级逐位置树** | **1.87** | **-45%**; 49 字样例贴住算术编码下限 |
| token 级 CE 下限 | ~1.7 | 算术编码的理论值(放弃前缀码, 未采用) |
| ts_zip 参照 (187 字, 需 GPU) | 2.45 | Bellard, RWKV-169M |

直观换算: 94 字群通知, 现状要吹 332 音符 ≈ 1.9 分钟; LM 档 167 音符
≈ 56 秒。

同样重要的四条负面结论(设计约束由此而来):

- 短消息(≲10 字)无增益甚至倒退(上下文冷启动) → **必须双档并存**;
- 任何数字损伤 → 模型上下文污染 → 后半段乱码, 无自愈 → **必须 RS 垫底**;
- 模型收益大头在 token 粒度(多字 token), 字符级方案吃不到 → **信源符号
  必须是 token**;
- 模型可用 SFT 注入游戏术语(时间/地点/角色/道具)——这是静态表永远没有
  的自由度, 也是本项目的差异化所在。

## 2. 目标 / 非目标

**目标**: 「LM 长文本档」进 GUI。≥30 字消息走 token 级编码, 双端 CPU
可跑, 压缩增益 ≥30% 数字/字, 与 RS 纠错整合后端到端可用。

**非目标**: 替换短消息字符档; 算术编码(失去前缀码的局部失步性质);
在线/自适应学习; 移动端; 多模型热切换。更换模型(更大/更偏中文)允许,
但必须满足 T2/T3 的接口与一致性契约。

## 3. 架构(一段话版)

```
编码: text → BPE 分词 → 逐 token [模型前向 → softmax → 定点化 → 七叉规范
      哈夫曼 → 码字] → 数字流 → RS 分帧纠错 → 旋转层 → 音符
解码: 音符 → 符号 → RS 修复 → 数字流 → 逐 token [同分布重建同一棵树 →
      解出 token ID → 前向] → 字节拼接 → 文本
```

两端能建出同一棵树的前提是**分布逐位一致**: softmax 后定点化(基数
Q=2^20, 下限平滑 1e-6)+规范码分配(同码长按词表序编号), 全部写死在契约
里; 模型权重哈希钉死——**模型版本即协议版本**, 换模型 = 换档位协商。
前缀码性质保留: 解码端永不崩溃, 损伤被 RS 拦在模型之前。

## 4. 任务分解

> 工作量为熟手估计; 每个任务独立可交付。认领: 在任务标题后署名。

### T1 `lm_codec.py` 模块化 【核心, 可立即开始】(3~5 人日, Python)

- 把 `exp_minimind/exp_tokens.py` 整理成正式模块: encode/decode CLI、
  selftest(往返/半码截断/失步再同步), 旋转层复用 `huffman_codec`;
- 流式解码器, 语义对齐 `receiver.py` 的 Receiver(feed/result/边界收尾);
- 验收: selftest 全绿; 94 字样例 167 数字 ±2 与实验对账。
- 预习顺序: README → `huffman_codec.py` 模块注释 → `exp_minimind/exp_tokens.py`。

### T2 推理后端: GGUF + llama.cpp (3~8 人日, C++/构建)

- minimind-3 → GGUF 导出, llama.cpp CPU 单线程跑通 next-token logits;
- **数值对照**: 与 numpy 参考实现逐 token logits 相对误差 < 1e-3
  (两端建同树的性命攸关前提);
- 后端适配器接口 `forward(tokens) -> logits`, 供 T1/T3 注入;
- 产出: 构建运行文档 + ms/token 基准(目标 < 50ms/token, numpy 参考 230ms)。

### T3 两端一致性协议 (2~4 人日, 测试向)

- 定点化契约文档化 + 可执行校验(同分布 → 同树 → 同数字流的断言测试);
- 跨机验证: A 机编码 → B 机解码(不同 CPU/后端), 全样本 roundtrip;
- 模型版本协议: 权重哈希计算与档位协商约定, 写入 README 信道约定节。

### T4 RS 帧对接 + 损伤仿真 (3~5 人日, Python)

- `lm_codec` 数字流 ↔ `rs_codec` 帧层对接: 编码端出数字进分帧, 解码端
  先修复再过模型;
- 蒙特卡洛: 吞音/多音/弹错率 × RS 档位扫描, 产出「生存率-码长」曲线
  (工具参考 `rs_codec.py simulate`);
- 验收: medium 档在 2%+1% 损伤下端到端正确率不低于字符档现有水平。

### T5 GUI 集成 + 双档切换 (3~5 人日, Python/Tk)

- receiver/GUI 接入 lm 档; 长度阈值自动切换(短→字符档, 长→LM 档, 可配);
- 流式解码延迟预算: 解码速度必须 ≥ 演奏速度(模型逐 token 前向), UI 呈现
  解码进度;
- 验收: 实录音频端到端(扩展 `e2e_demo.py`), GUI 截图/演示。

### T6 SFT 游戏语料 (5~10 人日, ML, 需 GPU 机器)

- 语料: 模板+槽位合成(时间/地点/角色/道具/动作) ≥5k 条, 有真实游戏内
  聊天记录更佳; 通用语料混训防灾难遗忘;
- LoRA 微调 minimind-3 (用户 GPU 机);
- 评测: 黑话组/通用组 bits/字 与 数字/字 before/after (harness 现成);
- 验收: 黑话组 -30% 以上, 通用组回退 <5%; 产出新 checkpoint + 数据报告。

### T7 tokenizer 手术: 术语入词表 【二期】(5~8 人日, ML)

高频术语(堵桥/团名/缩写)作为新 token 加入词表, embedding 用组成 token
均值初始化 + 短 SFT。一术语一符号 = 1~2 数字封顶。依赖 T6 的语料与流程。

### T8 DP 最优分解 【二期】(2~4 人日, 算法)

贪心 BPE 分解非全局最优; 但正确目标是**模型码长**而非 token 数(反例:
生僻合并 token 比高频拆分更贵)。做法: 复用编码时逐位置分布作边权, 词表
trie 上最短路 + EM 迭代(编码端 ~2× 前向, 解码端零改动——分解是编码端
私有的, 解码只认 ID 流)。依赖 T1+T6 完成后测增益, >3% 再考虑 beam 版。

## 5. 里程碑与依赖

```
M1 本地往返 demo     = T1 + T2
M2 双机互通         = M1 + T3
M3 带纠错端到端      = M2 + T4
M4 GUI 可用         = M3 + T5
M5 游戏术语版        = M4 + T6 (T7/T8 二期)
```

依赖: T1 → (T3, T4, T5); T2 → T3; T6 独立并行, 完成后反哺版本钉死。
T1 与 T2 是当前最大并行度所在, 可立即同时开工。

## 6. 协作约定

- 分支开发 + PR; 提交信息: 模块前缀 + 中文一句话(参考 `git log`)。
- 提交前 selftest 全绿: `huffman_codec.py` / `receiver.py` / `rs_codec.py`
  / (T1 起) `lm_codec.py`。
- 实验脚本入 `exp_minimind/`(权重不入库); 正式模块放仓库根目录;
  压缩率变化必须附同口径样本测量——数字说话。
- 涉及协议/接口(定点化契约、档位协商、码表格式)的改动, 先讨论定稿再动手。
